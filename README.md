Here is a 5-line `README.md` summary based on your detailed file:

# 🎓 Student Admission Portal (JSF + Hibernate)

A complete Student Admission Management System using **JSF** for the frontend and **Hibernate ORM** for database persistence. This project demonstrates core Java EE concepts, including navigation, managed beans, custom validators, and full CRUD operations.

**Tech Stack:** Java 8, JSF 2.3, Hibernate 5.6, Maven, and an embedded H2 database (no setup required).

**To Run:**

```bash
mvn clean package && mvn jetty:run
```

**Access:** `http://localhost:8080/student-admission-portal/welcome.xhtml`
